NextGen Studio source is MIT licensed. The Rust binary uses egui/eframe, glow, winit, rfd, image, serde, encoding_rs, tempfile and their transitive dependencies. Each package includes licenses/DEPENDENCIES.json with exact versions, license expressions, authors and upstream URLs, together with license/notice files found in the corresponding registry source distributions. Cargo.lock records dependency versions and checksums. System libraries and the separate NextGen client keep their own licenses. Qt is not linked or distributed in Rust packages. No source or game artwork from the reference editors is included.
